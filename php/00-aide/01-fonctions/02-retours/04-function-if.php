<?php
/**
 * Toujours dans du complexe mais pas délirant
 * 
 * On veut créer une fonction operatio() qui prend trois paramètres :
 * - $nombre1 qui représente le nombre 1
 * - $nombre2 qui représente le nombre 2
 * - $type qui représente l'opération que l'on veut faire (soit "addition", soit "multiplication", soit "division")
 * 
 * En gros : 
 * - si le paramètre $type contient "addition", on retourne $nombre1 + $nombre2
 * - si le paramètre $type contient "multiplication", on retourne $nombre1 * $nombre2
 * - si le paramètre $type contient "soustraction", on retourne $nombre1 - $nombre2
 * - si le paramètre $type contient "division", on retourne $nombre1 / $nombre2
 * 
 * Indice, on peut faire ça avec 4 if .. ou avec un seul switch, c'est vous qui voyez
 * 
 * 1) Créez la fonction operation()
 */

/**
 * 2) Créez une variable $resultat qui contient le retour de la fonction
 * operation() en lui demandant de multiplier 2 et 8.
 * Faites un var_dump de $resultat afin de vérifier qu'il contient bien 16 !
 */

/**
 * 3) Créez une variable $resultat qui contient le retour de la fonction
 * operation() en lui demandant de diviser 5 par 2 
 * Faites un var_dump de $resultat afin de vérifier qu'il contient bien 2.5
 */
 
/**
 * 4) La même chose mais en demandant de soustraire 15 à 25 et en vérifiant 
 * qu'on a bien 10 !
 */