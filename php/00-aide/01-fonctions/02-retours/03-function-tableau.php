<?php
/**
 * Allez, un peu plus complexe, mais pas délirant
 * 
 * On veut créer une fonction calculMontants()
 * qui prend en paramètre un nombre qui représente un montant HT
 * 
 * La fonction doit renvoyer UN TABLEAU qui contient 3 éléments :
 * "montantHT" => le montant HT
 * "montantTVA" => le montant TVA
 * "montantTTC" => le montant TTC
 */
 
/**
 * 1) Définissez la fonction calculMontants()
 */
 
/**
 * 2) Créez une variable $montant qui contient 100
 */

/**
 * 3) Créez une variable $montants qui contient le retour de la fonction
 * calculMontants à qui on passe en paramètre la variable $montant
 */
 
/**
 * 4) Faites un var_dump() de la variable $montants calculée juste avant
 * Si elle vous affiche un tableau avec 3 éléments :
 * - montantHT => 100
 * - montantTVA => 20
 * - montantTTC => 120
 * Alors tout est ok :-)
 */