<?php
/**
 * En PHP, la syntaxe pour écrire une fonction est assez simple
 * 
 * Nous allons l'étudier en créant plusieurs fonctions
 * 
 * Créer (définir) une fonction, c'est avant tout :
 * - utiliser le mot clé FUNCTION
 * - donner un NOM à la fonction
 * - utiliser les parenthèses (nous verrons ça plus en détails plus tard)
 * - utiliser les accolades pour définir le bloc de code à exécuter
 */
 
/**
 * Créons une fonction qui va afficher "Hello World"
 * 
 * On utilise donc le mot clé FUNCTION, puis
 * on nomme la fonction "helloWorld", on utilise
 * les parenthèses, puis on ouvre les accolades.
 * 
 * Ensuite on écrit notre bloc de code à exécuter
 * Et enfin, on referme les accolades !
 */
function helloWorld(){
    echo "Hello World !";
}

/**
 * Appelons la fonction maintenant afin qu'elle s'exécute !
 * 
 * Pour ce faire, nous devons simplement nommer la fonction que l'on veut appeler
 */
helloWorld() // Va afficher "Hello World !"
/**
 * Testez ce fichier dans votre navigateur
 */

/**
 * A VOUS ! 
 * 1) Créez / définissez une fonction qui s'appelle "direAurevoir" et qui va afficher "Aurevoir tout le monde !"
 */
 
/**
 * 2) Appelez la fonction afin qu'elle s'exécute
 */
 
/**
 * 3) Testez le tout en ouvrant ce fichier dasn votre navigateur
 * Il n'y a pas 36 solutions : soit votre page affiche bien "Au revoir tout le monde" et donc ça marche
 * Soit elle n'affiche pas, et donc vous avez foiré !
 */
?>